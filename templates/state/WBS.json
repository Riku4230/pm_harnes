{
  "tasks": [],
  "_schema_hint": "Each task: {id(T001), name(required), status(not_started|in_progress|done|blocked), owner, due(YYYY-MM-DD), depends_on([id]), subtasks([{id,name,status,due,depends_on}])}"
}
