{
  "tasks": [],
  "_schema_hint": "Each task: {name(required), status(required: not_started|in_progress|done|blocked), owner, due(YYYY-MM-DD), start_date(YYYY-MM-DD), dependencies([task_name]), description}"
}
